[build-system]
requires = ["setuptools>=64", "setuptools-scm>=8"]
build-backend = "setuptools.build_meta"

[project]
name = "lume-cheetah"
dynamic = ["version"]
description = "Cheetah-specific implementation of LUMEModel classes for virtual accelerators"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "Apache-2.0"}
authors = [
    {name = "SLAC National Accelerator Laboratory"}
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering",
]
dependencies = [
    "lume-base @ git+https://github.com/slaclab/lume-base",
    "cheetah-accelerator"
]

[project.urls]
Homepage = "https://github.com/lume-science/lume-cheetah"
Repository = "https://github.com/lume-science/lume-cheetah"
Documentation = "https://github.com/lume-science/lume-cheetah"

[project.optional-dependencies]
dev = [
    "pytest",
    "ipykernel",
    "pre-commit"
]
docs = [
    "pygments",
    "mkdocs",
    "mkdocstrings",
    "mkdocstrings-python",
    "mkdocs-material",
    "livereload",
    "pytkdocs[numpy-style]",
]

[tool.setuptools.packages.find]
include = [ "lume_cheetah", ]
namespaces = false

[tool.setuptools.package-data]
virtual_accelerator = ["py.typed"]

[tool.setuptools_scm]
version_file = "lume_cheetah/_version.py"

[tool.ruff]
# select = []
# ignore = []
