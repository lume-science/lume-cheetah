[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "lume-cheetah"
version = "0.0.1"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
include = ["lume_cheetah*"]